<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zpj.mapper.WorkerMapper">
    <insert id="insert">
        insert into WORKER (ACCOUNT, PASSWORD) VALUES (#{account},#{password})
    </insert>

</mapper>